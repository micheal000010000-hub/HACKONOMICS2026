## Packages
framer-motion | Complex layout animations and interactive elements
react-markdown | Rendering rich text responses from the AI tutor
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
}

Colors need to support the "Blockchain Blue" vs "Traditional Green" split theme.
